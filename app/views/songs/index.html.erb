<ul>
<% @songs.each do |song| %>
  
  <% if song.artist %>
    <%= link_to song.artist.name, song_path(song) %>
  <% else %>
    <%= link_to "Add Artist", edit_song_path(song) %>
  <% end %>
  
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>

<% if @song.artist %>
  <%= link_to @song.artist.name, artist_path(@song.artist) %>
<% else %>
  <%= link_to "Add Artist", edit_song_path(@song) %>
<% end %>